.DS_Store
__pycache__/
*.pyc
.env
node_modules/
.npm-cache/
.netlify

# Build output
dist/

# Generated archives
*.zip

# Local debug
debug/
debug.txt

# Local archive (orphans + scratch)
_archive/

# WordPress theme (deployed separately to VPS)
wp-theme/

# Python build scripts (not part of static site)
*.py

# Blog generation payloads — written to STATE_DIR now, but never deploy any legacy
# in-tree copies (raw prompt/source JSON must not be publicly served)
blog/*/_source.json

# Tailwind build intermediates
src/tw.css.bak

# Private outreach files — never deploy
white-label-email-template.html
send-emails/node_modules/

# VPS deploy artifacts (generated, contain base64 blobs)
_cf_deploy.php
_cf_content.php
_do_console_deploy.txt
_deploy_commands.txt

# Security: unauthenticated WP setup helper — never deploy
_cf_setup.php
_cf_setup.b64
tk_home.html
